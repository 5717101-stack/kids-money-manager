# FastAPI and Server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pydantic==2.10.0
pydantic-settings==2.6.1

# Google Gemini AI — pinned to stable release (avoid v1beta 404s)
google-generativeai==0.8.3

# Environment Management
python-dotenv==1.0.0

# Utilities
python-dateutil==2.8.2

# PDF Generation & Image Infographics
reportlab==4.0.7
python-bidi==0.4.2
Pillow>=10.0.0

# HTTP requests for Meta WhatsApp Cloud API
requests>=2.31.0

# Google Drive API (for meeting processing cron job)
google-api-python-client==2.144.0
google-auth==2.35.0
google-auth-httplib2==0.2.0

# Audio processing for speaker identification
pydub==0.25.1
webrtcvad==2.0.10

# Speaker Diarization & Voice Identification (pyannote.audio)
# NOTE: torch CPU is installed separately in render.yaml buildCommand to save ~1.5GB disk
pyannote.audio>=3.1.1
numpy>=1.24.0

# PDF parsing (robust, preserves layout/hierarchy)
PyMuPDF==1.24.3
pdfplumber==0.11.4

# Scheduling for cron jobs (weekly architecture audit)
apscheduler==3.10.4
