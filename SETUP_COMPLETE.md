# ✅ התקנה הושלמה!

הפרויקט הותקן בהצלחה במחשב החדש.

## מה הותקן:

1. ✅ **nvm (Node Version Manager)** - מנהל גרסאות Node.js
2. ✅ **Node.js v24.12.0** - גרסה LTS
3. ✅ **npm v11.6.2** - מנהל חבילות
4. ✅ **תלויות frontend** - כל החבילות הנדרשות
5. ✅ **תלויות backend** - כל החבילות הנדרשות

## איך להריץ את הפרויקט:

### אפשרות 1: הרצה מהירה (מומלץ)
```bash
cd "/Users/itzhakbachar/Family Bank/kids-money-manager"
npm run dev:all
```

### אפשרות 2: הרצה בשני חלונות נפרדים

**חלון 1 - Backend:**
```bash
cd "/Users/itzhakbachar/Family Bank/kids-money-manager/server"
npm run dev
```

**חלון 2 - Frontend:**
```bash
cd "/Users/itzhakbachar/Family Bank/kids-money-manager"
npm run dev
```

## כתובות:

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3001
- **Health Check**: http://localhost:3001/api/health

## הערות חשובות:

1. **nvm נטען אוטומטית** - בכל פעם שתפתח טרמינל חדש, nvm יטען אוטומטית
2. **MongoDB (אופציונלי)** - השרת יכול לעבוד עם MongoDB או עם אחסון זמני בזיכרון
   - אם יש לך MongoDB, צור קובץ `server/.env` עם:
     ```
     MONGODB_URI=mongodb://localhost:27017/kids-money-manager
     PORT=3001
     ```
3. **סיסמת הורה**: `2016`

## בדיקה שהכל עובד:

פתח טרמינל חדש והרץ:
```bash
node --version  # צריך להציג: v24.12.0
npm --version   # צריך להציג: 11.6.2
```

## אם יש בעיות:

1. **Node.js לא נמצא**: פתח טרמינל חדש (nvm נטען רק בטרמינלים חדשים)
2. **תלויות חסרות**: הרץ שוב:
   ```bash
   cd "/Users/itzhakbachar/Family Bank/kids-money-manager"
   npm install
   cd server && npm install
   ```

## קבצים שימושיים:

- `setup_new_computer.sh` - סקריפט התקנה למחשב חדש
- `QUICK_START.md` - מדריך התחלה מהירה
- `README.md` - תיעוד מלא

---

**הכל מוכן! תוכל להתחיל לעבוד על הפרויקט.** 🎉
